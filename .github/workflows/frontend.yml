name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install cypress --save-dev
        
      - name: Change directory and run Cypress tests
        run: cd tests/frontend && npx cypress run
